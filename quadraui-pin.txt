# quadraui pin — the single source of truth for "which quadraui is vimcode built against?"
#
# Why this file exists (#638)
# ---------------------------
# vimcode depends on quadraui through *relative sibling path dependencies*:
#
#     quadraui = { path = "../quadraui/quadraui", ... }
#     vt100    = { path = "../quadraui/vendor/vt100-0.16.2-patched" }
#
# Cargo does not pin path deps — `Cargo.lock` has no entry for them — so every
# build (local, agent, CI) silently compiles against whatever happens to be
# checked out in the neighbouring directory. That means an upstream quadraui
# merge can restate vimcode's behaviour with **zero vimcode commits**, and
# nothing in vimcode's history records that anything moved.
#
# That is not hypothetical. quadraui#472 hardcoded `char_cell_width == 2` for
# U+F0000..=U+F9999; the moment it landed on quadraui `develop` it staled six
# `tui_main::render_impl::tests::snapshot_*` tests on every machine at once.
# The failure was misdiagnosed for weeks as CI flakiness (#625, #615, #602),
# earned a seven-entry `--skip` list in CI, and that skip list then hid a real
# input-routing defect (#637) for its entire life.
#
# How the pin is enforced
# -----------------------
# `build.rs` resolves the sibling quadraui checkout, reads its `HEAD`, and
# compares it to the rev below. A mismatch is a **hard build failure** naming
# quadraui as the variable, not a silent behaviour change. The resolved rev is
# also baked into the binary and printed by `vimcode --version` / `vcd
# --version`, so "which quadraui?" is the first question the output answers
# rather than the last one anyone thinks to ask.
#
# `.github/workflows/ci.yml` checks out this exact rev too, so CI and local
# machines cannot silently disagree.
#
# Co-development escape hatch
# ---------------------------
# quadraui and vimcode are frequently co-developed and a hard pin must not make
# that impossible. Two escapes, in order of preference:
#
#   1. Uncommitted edits in the quadraui checkout are always allowed. As long
#      as quadraui's `HEAD` is the pinned rev, a dirty working tree only emits
#      a warning — edit the sibling and rebuild, exactly as before.
#
#   2. To build against a *different* quadraui commit (a feature branch, a
#      bisect, a local `develop`), set:
#
#          VIMCODE_QUADRAUI_UNPINNED=1 cargo build
#
#      The build then only warns, naming both revs. Never set this in CI, and
#      never leave it exported in a shell you also run `cargo test` in — a
#      stale-quadraui snapshot diff looks identical to a real regression.
#
# Bumping the pin
# ---------------
# Bumping is a deliberate, reviewable commit — that IS the point of this file:
#
#     cd ../quadraui && git checkout develop && git pull && git rev-parse HEAD
#     # paste the sha below, then:
#     cargo test          # snapshots re-run against the new quadraui
#     git commit quadraui-pin.txt -m "chore: bump quadraui pin to <sha short>"
#
# If `cargo test` goes red on that commit, the redness is *attributable*: the
# diff is one line and the cause is named in it.
#
# See also: docs/QUADRAUI_GUIDE.md, #638, #625, quadraui#472, quadraui#529.
#
# `coord-tui` has the identical exposure on the same path dep and should carry
# a mirror of this file; see docs/QUADRAUI_GUIDE.md.
#
# ---------------------------------------------------------------------------
# Format: the first non-comment, non-blank line is a full 40-char quadraui SHA.
# ---------------------------------------------------------------------------
#
# 3ad869a — "fix(quadraui#595): gate the sidebar_reveal examples on their
# backend feature". Bumped by vimcode#659 to adopt the two helpers that were
# *promoted out of this repo* into quadraui:
#
#   * quadraui#594 (26c7cde) — `TabBarLayout::tab_center` /
#     `tab_close_center`, plus `TuiDriver` / `GtkDriver` wrappers keyed by the
#     tab bar's `WidgetId`. `src/gtk/testing.rs`'s private `tab_center` /
#     `tab_close_center` (#553) were the literal source for that promotion, so
#     this bump is what lets vimcode delete its copy instead of maintaining a
#     second one that drifts.
#   * quadraui#595 (c6c9556, 02aae99, 3ad869a) — `SidebarSystem::reveal`,
#     the generic "select + expand + scroll into view" composition whose
#     reference implementation is `Engine::explorer_reveal_path`. NOTE: it
#     landed on `SidebarSystem` only; there is no `TreeController::reveal`,
#     and vimcode's explorer drives a bare `TreeController`, so the vimcode
#     side of that adoption is still blocked — see #659's closing note.
#
# **Eighty quadraui commits ride along in f6d27c2..3ad869a** and only three of
# them are the two features above. This is by far the largest single pin move
# to date; the rest is quadraui's own #493 macOS driver work, #541 tooltip
# chrome, #555 vt100 conformance observer, #467 `drag_and_modal_mut`, #544
# sidebar-panel routing and a long tail of review fixes. Nothing in that tail
# touches an API vimcode calls (the full `cargo test` suite is green on it),
# but "green suite" is not "smoke-tested" — the tooltip and sidebar-panel
# commits both move painted chrome, so exercise tooltips and sidebar panel
# clicks on a live display before treating this rev as settled.
#
# Previous pin: f6d27c2 — "fix(quadraui): address #552 review — downstream
# impact + dangling
# test refs". Bumped by vimcode#650 to adopt quadraui#552, which pins the TUI
# activity bar's hit regions to bar-relative space. Before it, the TUI
# rasteriser returned *absolute* rows while GTK/macOS/the no-paint helper
# returned rect-relative ones; `AppShell::cached_activity_hit` added the bar's
# `ab.y` on top, so the moment the menu bar was revealed at runtime every
# activity-bar hit region shifted down one row and clicking Search opened
# Explorer. That is vimcode#634's recurring smoke bug, and its regression
# guard — `tui_main::shell_app::tests::
# menu_reveal_then_search_icon_click_opens_search_not_explorer` — is
# un-ignored by the same commit that moves this line.
#
# Seven quadraui commits ride along in f702422..f6d27c2; only 7c8209d is the
# activity-bar fix. Three (c66590d, e60d1c3, e31258f) are quadraui#550 —
# `DataTableLayout::hit_test` made `h_scroll`-aware, `h_scroll` rounded for the
# TUI so hit-test agrees with paint, and `Backend::data_table_layout` routed
# through the TUI resolver. That is a *second* hit-test behaviour change, in a
# different widget, with no coverage in this repo: smoke a horizontally
# scrolled DataTable, not just the activity bar. Two (adb5f7b, b70d200) pin
# quadraui's own CI toolchain to 1.97.1 via *quadraui's* `rust-toolchain.toml`;
# that file is a rustup per-directory override and quadraui is a *sibling*, not
# an ancestor, of this checkout, so it does not move vimcode's toolchain.
#
# Pin before that: f702422 — "fix(quadraui): honour ShellConfig::
# title_bar_height_lh regardless of has_title_bar (#547)". First rev carrying
# every trait/API addition the open vimcode work depends on: quadraui#531
# draw_settings_chrome, #532 AppShell::set_title_bar_visible, #533
# draw_terminal_divider, #537 WinBackend parity, #545 char_cell_width, #547
# title_bar_height_lh.
3ad869a89f10bf324f6b9ead33f4ad6ce7d203b8
