# Driver mock fixtures for this milestone (vimcode's configured glob: `*.screen`).
#
# `ms-example` is the #657 reachability slice — it drives the live harnesses
# directly and needs no mocks, so this directory is intentionally empty. A real
# `ms-NN/mocks/` holds the recorded screens its slice asserts against.
