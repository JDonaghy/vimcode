# YAML Language Support

Language support for YAML — formatting, validation, and schema support.

**LSP**: `yaml-language-server` — `npm install -g yaml-language-server`
**File types**: `.yml`, `.yaml`

## Install

```
:ExtInstall yaml
```
