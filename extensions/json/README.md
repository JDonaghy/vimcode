# JSON Language Support

Language support for JSON — formatting, validation, and schema support.

**LSP**: `vscode-json-languageserver` — `npm install -g vscode-langservers-extracted`
**File types**: `.json`, `.jsonc`, `.json5`

## Install

```
:ExtInstall json
```
