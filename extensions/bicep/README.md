# Bicep Language Support

Language support for Azure Bicep infrastructure-as-code files.

**LSP**: `bicep-langserver` — `dotnet tool install -g bicep-langserver`
**File types**: `.bicep`, `.bicepparam`

## Install

```
:ExtInstall bicep
```
