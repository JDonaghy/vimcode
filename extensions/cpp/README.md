# C / C++ Language Support

Language support for C and C++ — diagnostics, completions, go-to-definition, debugger.

**LSP**: `clangd` — `sudo apt-get install -y clangd`
**DAP**: codelldb
**File types**: `.c`, `.h`, `.cc`, `.cpp`, `.cxx`, `.hh`, `.hpp`, `.hxx`

## Install

```
:ExtInstall cpp
```
